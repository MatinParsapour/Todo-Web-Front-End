import { SummaryPipesPipe } from './summary-pipes.pipe';

describe('SummaryPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new SummaryPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
